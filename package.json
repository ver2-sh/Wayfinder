{
  "name": "project-wayfinder",
  "version": "0.1.0",
  "private": true,
  "description": "Authenticated MCP bridge to local shell execution",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": "^24.0.0 || ^26.0.0"
  },
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch --experimental-strip-types src/index.ts",
    "build": "tsc",
    "start": "node --env-file-if-exists=.env dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/node": "^2.0.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^26.5.0",
    "typescript": "^7.0.2"
  }
}
